[project]
name = "search-for-invite-bd"
version = "0.1.0"
description = "OpenAI Agents SDK workflow for discovering city-specific LinkedIn profiles using Bright Data SERP and Web Unlocker."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "beautifulsoup4>=4.12.3",
    "eval-type-backport>=0.2.2; python_version < '3.10'",
    "openai-agents>=0.5.0",
    "pydantic>=2.8.0",
    "python-dotenv>=1.0.1",
    "requests>=2.32.3",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sse-starlette>=2.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
]

[project.scripts]
invite-finder = "invite_finder.cli:main"
invite-api = "invite_finder.api.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/invite_finder"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
